% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_con.R
\name{dr_con_raw}
\alias{dr_con_raw}
\title{Connect to Raw ICES DATRAS Tables}
\usage{
dr_con_raw(table = "HH")
}
\arguments{
\item{table}{A character string specifying the table. One of \code{"HH"} (default),
\code{"HL"}, \code{"CA"}, \code{"FL"}, \code{"LT"}, \code{"CPUEL"}, \code{"CPUEA"}, \code{"CW"}, or \code{"IDX"}.}
}
\value{
A lazy \code{duckdbfs} tibble. Pipe dplyr verbs and call \code{\link[dplyr:compute]{dplyr::collect()}}
to bring data into memory.
}
\description{
Opens a lazy DuckDB connection to "as-is" DATRAS parquet files — the raw
tables as downloaded from the ICES datacenter, with original old-style column
names (e.g. \code{Ship}, \code{HaulNo}, \code{ShootLat}). Use this when you need unmodified
ICES output rather than the tidied versions provided by \code{\link[=dr_con]{dr_con()}}.
}
\examples{
\dontrun{
  dr_con_raw("FL") |> dplyr::glimpse()

  dr_con_raw("FL") |>
    dplyr::filter(Survey == "NS-IBTS", Year == 2020) |>
    dplyr::collect()
}
}
