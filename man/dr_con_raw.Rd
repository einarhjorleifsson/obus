% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_con.R
\name{dr_con_raw}
\alias{dr_con_raw}
\title{Connect to a raw DATRAS exchange table}
\usage{
dr_con_raw(table, path = opus::op_archive(), quiet = TRUE)
}
\arguments{
\item{table}{One of \code{"HH"}, \code{"HL"}, \code{"CA"}, \code{"LT"}.}

\item{path}{Archive root -- a directory named \code{raw}, local or remote.
Defaults to \code{opus::op_archive()}, which honours
\code{getOption("opus.archive")} and falls back to the published archive.}

\item{quiet}{Logical. If \code{FALSE}, report what was connected to.}
}
\value{
A lazy \code{tbl}. Pipe \code{{dplyr}} verbs and call
\code{\link[dplyr]{collect}}.
}
\description{
Opens a lazy DuckDB connection to one of the four Tier-1 exchange tables in
the raw opus archive. These carry opus's current field names exactly as
staged, with no obus-side additions -- in particular no \code{.id} (see
\code{\link{dr_add_id}}) and no \code{Valid_Aphia}/\code{SpeciesSex} rename.
}
\examples{
\dontrun{
  dr_con_raw("HH") |>
    dplyr::filter(Survey == "NS-IBTS", Year == 2022) |>
    dplyr::collect()
}
}
\seealso{
\code{\link{dr_con}} for the tables obus derives from these.
}
