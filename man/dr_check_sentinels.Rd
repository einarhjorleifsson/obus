% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_check.R
\name{dr_check_sentinels}
\alias{dr_check_sentinels}
\title{Check for -9 sentinel values remaining in numeric columns}
\usage{
dr_check_sentinels(d, table_label = NULL, flag = FALSE)
}
\arguments{
\item{d}{A data frame or \code{tbl_duckdb_connection}.}

\item{table_label}{Label for the \code{table} column in the result.
Defaults to the name of \code{d}.}

\item{flag}{Logical. If \code{FALSE} (default) return a one-row summary tibble.
If \code{TRUE} return the input data with a \code{.pass} column added (\code{TRUE} = no
\code{-9} in any numeric column on that row, \code{FALSE} = at least one hit).}
}
\value{
A one-row summary tibble, or the input data with \code{.pass} added.
}
\description{
In raw DATRAS exchange data, \code{-9} represents a missing/inapplicable field.
obus fetchers replace \code{-9} with \code{NA} after download. This function scans all
numeric and integer columns for any surviving \code{-9} values, which would
indicate that the replacement step was bypassed or a new fetcher path is
missing it.
}
\details{
Safe to run on any DATRAS table (HH, HL, CA, etc.) or any data frame.
}
