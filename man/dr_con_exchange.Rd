% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr_connection.R
\name{dr_con_exchange}
\alias{dr_con_exchange}
\title{Create a DuckDB connection to a DATRAS dataset (exchange format)}
\usage{
dr_con_exchange(type = NULL, url = "https://heima.hafro.is/~einarhj/datras/")
}
\arguments{
\item{type}{A character string specifying the type of dataset. Must be \code{"HH"}, \code{"HL"}, or \code{"CA"}.
This parameter maps to specific file types stored in the DATRAS data source.}

\item{url}{The http path to the DATRAS parquet files}
}
\value{
A DuckDB dataset object representing the combined data for the specified \code{type}.
}
\description{
This function establishes a DuckDB connection to DATRAS exchange-format data based on the specified \code{type}.
The dataset is accessed via a URL and opened using the \code{duckdbfs::open_dataset} function.
}
\section{Datapath Explanation}{
The function constructs a URL for the dataset as:
"https://heima.hafro.is/~einarhj/datras/RecordType={type}/Year={year}/part-0.parquet".
The \code{type} parameter determines the file category (e.g., "HH", "HL", "CA").
The function dynamically generates URLs for all years between 1965 and 2025.
The DATRAS exchange dataset includes the following:
\itemize{
\item \code{"HH"} refers to haul-level data.
\item \code{"HL"} refers to catch-at-length data.
\item \code{"CA"} refers to age-based biological sampling data.
}
}

\examples{
\dontrun{
  dr_con_exchange("HH")  # Connect to haul-level data for all years.
  dr_con_exchange("CA")  # Connect to age-based biological data for all years.
}
}
