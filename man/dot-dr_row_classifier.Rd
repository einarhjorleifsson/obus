% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.dr_row_classifier}
\alias{.dr_row_classifier}
\title{Row-Wise Value Classifier}
\usage{
.dr_row_classifier(...)
}
\arguments{
\item{...}{A series of numeric, logical, or categorical vectors to classify.
All vectors must be of the same length.}
}
\value{
A character vector, with one element per row of input, containing the concatenated
classifications for each row.
}
\description{
This function classifies the values in a set of input vectors row-wise based on specific rules.
It supports numeric, logical, and categorical (non-numeric) vectors, applying type-specific
classifications and producing a combined classification for each row as a delimited string.
}
\details{
\subsection{Classification Rules:}{
\itemize{
\item \strong{Numeric or Logical Variables}:
\itemize{
\item \code{NA}: Missing values are classified as \code{"NA"}.
\item \code{0}: Values equal to \code{0} (or \code{FALSE} for logicals) are classified as \code{"0"}.
\item \verb{>0}: Positive values (or \code{TRUE} for logicals) are classified as \code{">0"}.
\item \verb{<=0}: Negative values are classified as \code{"<=0"}.
}
\item \strong{Categorical (Non-Numeric) Variables}:
\itemize{
\item \code{"a"}: Non-missing values are classified generically as \code{"a"}.
\item \code{"NA"}: Missing values are classified as \code{"NA"}.
}
}
}

\subsection{Output:}{

The function outputs a combined classification for each row, concatenated using \code{"_"}
to separate the classifications for all input vectors.
}
}
\examples{
var1 <- c(NA, 0, 1, 2)           # Numeric
var2 <- c(0, 1, NA, 3)           # Numeric
var3 <- c(NA, NA, 2, 0)          # Numeric
var4 <- c("a", "b", NA, "c")     # Categorical
var5 <- c(TRUE, FALSE, NA, TRUE) # Logical

classifications <- .dr_row_classifier(var1, var2, var3, var4, var5)
print(classifications)

## Output:
# [1] "NA_0_NA_a_>0" "0_>0_NA_a_0" ">0_NA_>0_NA_NA" ">0_>0_0_a_>0"

}
